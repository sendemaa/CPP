#include <iostream>
#include <string>
using namespace std;


int main() {
    setlocale(0, "Russian");
    string names[10] = {
        "ноль", "один", "два", "три", "четыре","пять", "шесть", "семь", "восемь", "девять"};

    int n;
    cout << "Введите число от 0 до 9: ";
    cin >> n;

    if (n >= 0 && n <= 9) {
        cout << "Название: " << names[n] << endl;
    } else {
        cout << "НЕТ. ВВЕДИТЕ ЧИСЛО ОТ 0 ДО 9!!!!!!" << endl;
    }

    return 0;
}
